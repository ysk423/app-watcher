# Google Play App Watcher 要求仕様書

## 1. 概要

Google Playで公開されているAndroidアプリを、ユーザーが指定した監視対象について定期的に収集・保存し、過去との差分や傾向を確認できるWebアプリを構築する。

収集したデータはCloudflare D1(SQLite互換のサーバーレスDB)に履歴として保存し、Google Gemini APIを利用して日次のAI分析およびWeb UIからの質問応答を行う。

本システムは、**VM/VPSを常時稼働させず**、Cloudflare Workers上でサーバーレスに動作することを前提とする。**Cloudflareの無料プランの範囲内で運用できること**を必須要件とする(Workers Paidプラン等の有料機能は前提としない)。

---

## 2. システムの目的

### 2.1 主目的

- Google Play上の指定アプリを継続的に監視する
- アプリ情報の変化を日次で記録する
- レビューの変化を追跡する
- アプリ間比較や更新内容、評価傾向をAIで分析する
- Webブラウザから過去データ・AI分析結果を確認する
- Web UIから自然言語でAIに質問できるようにする

### 2.2 対象外

以下は初期バージョンでは実装しない。

- 自動的な競合アプリ発見
- ローカルLLMの実行
- Discord等への通知
- Google Playへのアプリ公開・管理操作
- 画像ファイルの恒久保存
- Google Playページ全体のHTMLアーカイブ
- 動画等のバイナリ保存
- Cloudflare Workers Paidプラン以上を前提とする機能(Queues, Durable Objects, Logpush, R2等)

---

## 3. 動作環境

### 3.1 想定実行基盤

- Cloudflare Workers(サーバーレス、無料プラン)
- Cloudflare D1(SQLite互換DB、無料プラン)
- Cloudflare Cron Triggers(定期実行、無料プランでも利用可能)

VM/VPSのようなサーバーの調達・OS管理・常時稼働は行わない。リソース制約は「500MB RAM」ではなく、**Workers無料プランのリクエストあたりCPU時間制限(目安10ms)・1日あたりリクエスト数上限(目安10万件/日)・サブリクエスト数上限**等に置き換わる。これらの制約を超えないよう、処理を小さな単位に分割することを最優先とする。

### 3.2 想定構成

```text
Internet
   |
   | HTTPS (Cloudflareが自動提供)
   v
Cloudflare Workers (Hono)
   |
   +---- Basic Authentication (Worker内ミドルウェア)
   |
   +---- Cloudflare D1
   |
   +---- Google Play Data Collector (fetch)
   |
   +---- Gemini API Client (fetch)

Cloudflare Cron Triggers
   |
   v
Workers scheduled handler ---- D1 (収集キュー/カーソル管理)
```

### 3.3 推奨ソフトウェア構成

- Cloudflare Workers(TypeScript)
- Hono(軽量Webフレームワーク、SSR用JSXレンダラ・Basic Authミドルウェアを含む)
- Cloudflare D1
- Cloudflare Cron Triggers
- Wrangler(ローカル開発・デプロイ用CLI)
- Google Gemini API

具体的なライブラリ・バージョンは実装時点で安定版を選択する。Node専用API・ネイティブ依存のあるライブラリはWorkers実行環境で動かないため、極力`fetch`ベースの自前実装で完結させる。

---

## 4. 監視対象アプリ

### 4.1 登録方法

監視対象アプリはユーザーが手動で登録する。

自動的な競合アプリ発見機能は実装しない。

基本的な識別子としてGoogle Playのパッケージ名を使用する。

例:

```text
com.example.app
```

### 4.2 登録時

ユーザーがパッケージ名を入力すると、Google Playから取得可能なアプリ情報を取得し、アプリを登録する。

### 4.3 上限

アクティブな監視対象は最大100アプリとする。

停止中のアプリはアクティブ数にカウントしない。

---

## 5. Google Playデータ収集

### 5.1 収集頻度

原則として1日1回。

標準実行時刻:

```text
03:00 JST
```

タイムゾーンは `Asia/Tokyo` を使用する。Cloudflare Cron Triggerの`crons`設定はUTCで指定するため、03:00 JST = 前日18:00 UTCとして設定する。

**Workers無料プランのCPU時間制限により、100アプリを1回のCron発火で処理しきれない可能性が高い。** そのため、03:00 JSTから一定の時間帯(例: 03:00〜04:00 JST)にかけて数分おきに複数のCron Triggerを設定し、各回はD1に保持した「本日分の未処理アプリキュー」からカーソルで少数ずつ取り出して処理する方式とする。1回あたりの処理件数は固定値を仕様として断定せず、**実装時にCPU時間を実測した上で調整する**(要検証事項)。全アプリの処理が完了したら、その日のキューを完了扱いとする。

### 5.2 収集対象

Google Playで公開されている情報のうち、技術的・規約上取得可能なものを収集する。

最低限、以下を対象とする。

- アプリ名
- デベロッパー名
- アプリアイコンURL
- カテゴリ
- 評価
- レビュー件数
- レビュー
- インストール数のレンジ
- 現在のバージョン
- 更新日
- What's New
- 価格
- アプリ内購入情報
- Android要件
- スクリーンショットURL
- その他Google Play上で公開されている有用な情報

画像そのものではなく、可能な限りURLを保存する。

### 5.3 Google Play取得方式

Google Play Developer APIは任意の公開アプリを取得する用途には適さないため、公開Google Playページ等から取得可能な情報を収集する方式を基本とする。

Workers実行環境ではNode専用の外部スクレイピングライブラリ(内部でNodeの`http`/`https`モジュールやネイティブ依存を使うもの)がそのまま動かない可能性が高いため、`fetch()`でアプリページのHTMLを取得し、ページ内に埋め込まれたJSONデータ(`AF_initDataCallback`等)を正規表現等で抽出する自前実装とする。

Google Play側のページ構造変更に対応しやすいよう、データ取得処理は独立したCollectorモジュールとして実装する。

取得方法はGoogleの利用規約・robots等を含む適用されるルールを遵守する。

### 5.4 取得失敗

Google Playからの取得に失敗した場合:

1. 前回正常取得したデータを保持
2. エラー内容をD1に記録
3. その日の取得を失敗扱いとする
4. 次回の定期実行で再試行

取得失敗を理由に過去データを削除してはならない。

---

## 6. 履歴管理

### 6.1 基本方針

日次スナップショットをCloudflare D1に保存し、時系列で変化を追跡できるようにする。

### 6.2 履歴対象

例えば以下を履歴として保存する。

- 評価
- レビュー件数
- バージョン
- 更新日
- What's New
- 価格
- アプリ内購入情報
- Android要件
- インストール数レンジ
- その他取得可能な主要情報

### 6.3 差分

前回取得データと比較し、変更された項目を判定できるようにする。

Web UIでは、

```text
バージョン
1.2.3 → 1.2.4

評価
4.21 → 4.24

レビュー数
12,345 → 12,501

What's New
変更あり
```

のように変更を確認できるようにする。

---

## 7. レビュー

### 7.1 保存対象

新しいレビューを取得する。

保存対象は原則として直近90日以内のレビューとする。

### 7.2 保存期間

レビューは永久保存しない。

90日を超えたレビューは削除対象とし、D1のストレージ使用量(無料枠)を抑制する。

ただし、レビューから生成された集計値・AI分析結果については、別途履歴として保持できる。

### 7.3 重複

同一レビューを重複保存しないよう、Google Play側で取得可能なレビューID等を利用して一意性を確保する。

---

## 8. 画像・大容量データ

以下のバイナリデータは保存しない。

- アプリアイコン画像
- スクリーンショット画像
- 動画
- Google Playページの完全HTML

原則としてURLのみ保存し、Web UIではリモートURLから表示する。

これにより、D1の無料枠(ストレージ・書き込み行数上限)内でも長期間運用できる構成とする。

---

## 9. データベース

Cloudflare D1(SQLite互換のサーバーレスDB)を使用する。

### 9.1 想定データ

最低限、以下の概念を管理する。

- monitored_apps
- app_snapshots
- reviews
- ai_analyses
- collection_jobs
- system_status
- collection_queue(日次収集をCron Trigger間で分割処理するためのキュー/カーソル)

実際のテーブル構成は実装時に正規化・性能を考慮して決定する。

### 9.2 トランザクション

Workerの実行が途中で終了(タイムアウト・エラー等)しても、D1が不整合にならないようトランザクション・冪等な書き込み(例: `INSERT OR REPLACE`)を適切に利用する。

### 9.3 無料枠

D1の無料プランには読み取り/書き込み行数・ストレージ容量の上限がある。上限に近づいた場合はWeb UIで確認できるようにし、超過を理由に自動的に有料プランへ移行してはならない。

---

## 10. AI分析

### 10.1 API

Google Gemini APIを使用する。

無料利用枠を前提とし、初期推奨モデルは:

```text
gemini-2.5-flash-lite
```

モデル名は設定(環境変数/Wrangler secrets、およびD1上の設定テーブル)から変更可能とし、将来モデルを変更する際にソースコードを大幅に変更する必要がない設計とする。

### 10.2 課金防止

本システムは無料利用を前提とする。

Geminiの無料枠・レート制限等を超過した場合に、自動的に有料利用へ移行してはならない。

無料枠・レート制限に達した場合は:

- AI分析をスキップまたは延期
- データ収集は継続
- AI分析を未実施・保留状態として記録
- 後から再実行可能にする

### 10.3 日次AI分析

Google Playデータの収集後、日次でAI分析を実行する。

分析内容には以下を含める。

- アプリの評判分析
- レビュー分析
- アプリ間比較
- アップデート分析
- トレンド分析
- 最近何が起きたかの要約
- 前日・過去期間との比較

収集と同様に、100アプリ分の分析をWorkers無料プランのCPU時間制限内で1回のCron発火に収めきれない可能性があるため、収集完了後に分析対象キューをD1に積み、複数回のCron発火に分割して処理する。

### 10.4 AIへの入力

毎回データベース全体をGeminiへ送信してはならない。

必要な対象・期間・差分のみをD1から抽出し、必要最小限のコンテキストをGeminiへ送信する。

---

## 11. Web UI AI Q&A

Web UIから自然言語で質問できる機能を提供する。

例:

```text
「この3か月で評価が一番改善したアプリは？」

「AアプリとBアプリで、最近のレビュー傾向はどう違う？」

「昨日のアップデートで何が変わった？」

「最近ユーザーから不満が増えている機能は？」
```

### 11.1 処理方式

Workers上でLLMを実行しない(Gemini APIを利用する)。

```text
Web UI
  ↓
D1から必要なデータを取得
  ↓
必要なコンテキストだけGemini APIへ送信
  ↓
回答をWeb UIへ表示
```

Web UIからの1回のQ&Aは1アプリの手動取得と同様に軽量な処理であり、Workers無料プランのCPU時間制限内で完結する想定とする。

### 11.2 制約

- 1回の質問でDB全体をGeminiへ送信しない
- コンテキスト量を必要最小限にする
- Geminiエラー時は適切なエラー表示
- 無料枠超過時に有料APIへ移行しない

---

## 12. Web UI

初期バージョンはWeb UIのみとする。SSR(サーバーサイドレンダリング)とし、SPA/クライアントサイドJSフレームワークは使用しない。

### 12.1 アプリ一覧

以下を表示する。

- アプリ名
- アイコン
- パッケージ名
- 現在のバージョン
- 評価
- レビュー件数
- 最終取得日時
- 監視状態
- エラー状態

### 12.2 アプリ詳細

アプリごとに以下を確認できる。

- 現在の情報
- 過去のスナップショット
- 各項目の推移
- What's Newの履歴
- レビュー
- AI分析履歴
- 取得エラー履歴

### 12.3 差分表示

前回取得時との差分を確認できるようにする。

### 12.4 監視操作

- アプリ追加
- 監視停止
- 監視再開
- 永久削除

を提供する。

---

## 13. アプリのライフサイクル

### 13.1 監視停止

監視を停止しても、過去データは削除しない。

停止したアプリはアクティブ監視対象数にカウントしない。

後から監視を再開できる。

停止中のアプリの履歴はWeb UIから閲覧可能とする。

### 13.2 永久削除

永久削除は監視停止とは別操作とする。

永久削除では原則として:

- アプリ情報
- スナップショット
- レビュー
- AI分析結果
- 取得履歴

を削除する。

誤操作防止のため、確認ダイアログを表示する。

### 13.3 Google Playから消えた場合

Google Playからアプリが消えた、または取得できなくなった場合でも自動削除しない。

例えば:

```text
⚠ 取得できません
最終取得成功: 2026-09-05
```

のように状態を表示し、過去データを保持する。

後日取得可能になった場合は自動的に監視を継続する。

---

## 14. 手動実行

Web UIから手動でデータ取得を実行できるようにする。

特にアプリ登録直後に利用できるようにする。

### 14.1 手動取得

```text
アプリ登録
   ↓
今すぐ取得
   ↓
初回データ保存
```

手動取得は対象1アプリのみを処理するため、Workers無料プランのCPU時間制限内で1回のHTTPリクエスト処理として完結させる。

### 14.2 手動AI分析

必要に応じてAI分析も手動実行できるようにする。

### 14.3 二重実行防止

同一アプリに対して収集中の場合は、重複した収集ジョブを開始しない。D1上のロック用テーブルに一意制約を設け、`INSERT OR IGNORE`等で排他制御を行う(Durable Objects等の有料機能は使用しない)。

---

## 15. 定期実行

Cloudflare Cron Triggersを利用する(無料プランでも利用可能)。

### 15.1 日次処理

```text
03:00 JST 前後(複数回のCron発火に分割)
  ↓
未処理キューからバッチ取得
  ↓
Google Play取得
  ↓
D1保存
  ↓
差分判定
  ↓
レビュー更新
  ↓
(キューが空になるまで次回Cron発火で継続)
  ↓
収集完了後、AI分析キューを積む
  ↓
AI分析(同様に複数回のCron発火に分割)
  ↓
AI分析結果保存
```

### 15.2 失敗時

取得とAI分析は独立したジョブとして扱う。

例えば:

```text
Google Play取得: 成功
D1保存: 成功
Gemini分析: 失敗
```

という状態を許容する。

---

## 16. 実行状態・監視

Web UIでシステムの稼働状況を確認できるようにする。

最低限、以下を表示する。

```text
最終取得       2026-09-08 03:02  ✓
最終AI分析     2026-09-08 03:08  ✓
次回取得       2026-09-09 03:00

取得エラー     0
AIエラー       1
```

また、各アプリについても:

- 最終成功取得日時
- 最終AI分析日時
- 現在の状態
- 最後のエラー
- エラー発生日時

を確認できるようにする。

---

## 17. 可用性・障害復旧

サーバーレス構成のため、VMの「再起動」という概念自体が存在しない。可用性はCloudflareのインフラに委ねられる。本システムが自前で担保すべき点は以下。

### 17.1 デプロイの安全性

`wrangler deploy`でのデプロイ失敗時、あるいはデプロイ後に不具合が判明した場合、直前のバージョンにロールバックできるようにする(Cloudflare Workersのバージョン管理・ロールバック機能を利用する)。

### 17.2 ジョブの冪等性

Cron Triggerによる定期ジョブ、および手動実行によるジョブは、途中で処理が中断されても再実行可能(冪等)であるように設計する。

### 17.3 異常終了

収集中・AI分析中にWorkerの実行が終了(タイムアウト・エラー等)した場合でも、

- D1を破損させない
- 実行中状態を永久に残さない(タイムアウトしたロックは一定時間後に解除可能とする)
- 次回実行時に再実行可能

とする。

### 17.4 二重実行防止

D1上のロック機構により、同じジョブが並列実行されないようにする。

---

## 18. ストレージ使用量管理

Cloudflare D1の無料プランにはストレージ容量・行数の上限がある。使用量を監視する。

Web UIにD1の使用量(概算)を表示する。

例:

```text
Storage
320 MB / (D1無料枠の上限)
```

### 18.1 警告レベル

- 70%: 警告
- 85%: 強い警告
- 95%: 危険状態

レビュー90日制限、画像バイナリ非保存等によりストレージ増加を抑制する。無料枠の具体的な上限値は実装時のCloudflareの提供条件に従う(将来変更されうるため断定しない)。

---

## 19. DBバックアップ

Cloudflare D1の**Time Travel**(組み込みのポイントインタイムリストア機能)を第一の手段として利用する。追加のバックアップファイル運用・外部ストレージへの転送は、無料プラン方針(有料のR2利用や支払い方法登録を避ける)に鑑みて必須とはしない。

### 19.1 保持期間

D1 Time Travelの保持期間は無料プランの提供条件に従う。要求仕様上の目安として「直近7日相当」を復元可能であることを期待するが、**実際の無料プランでの保持期間は実装時に確認し、不足する場合は代替手段(D1からのエクスポートをWeb UIから手動実行できるようにする等)を検討する**(要検証事項)。

### 19.2 バックアップ要件

バックアップ・復元の仕組みが失敗しても本体のデータ収集処理を停止させない。

Time Travelの利用可否・直近の状態もシステム状態として確認できることが望ましい。

---

## 20. ログ管理

Workersはファイルシステムを持たないため、ファイルベースのログローテーションは行わない。

- 収集・AI分析・定期ジョブの成功/失敗、エラー内容は、D1の`collection_jobs`/`system_status`相当のテーブルに記録し、Web UIから確認できるようにする
- リアルタイムのデバッグには`wrangler tail`(Cloudflareが提供するライブ実行ログ)を利用する
- 永続的なログ保存基盤(Logpush等)は有料機能のため使用しない。D1に記録された実行履歴が実質的な運用ログとなる

D1へのログ記録は行数増加につながるため、無期限に蓄積せず、古い実行履歴は一定件数/一定期間で削除する。

---

## 21. 認証・セキュリティ

### 21.1 HTTPS

外部公開するWeb UIはHTTPSを必須とする。Cloudflareが自動的にHTTPSを提供するため、証明書の取得・更新を自前で行う必要はない。

### 21.2 Basic Authentication

Web UIへのアクセスにはHTTP Basic Authenticationを使用する。リバースプロキシ(Nginx等)を用いないため、Worker内(Honoのミドルウェア)で認証処理を行う。

```text
Internet
   ↓ HTTPS(Cloudflare提供)
Cloudflare Workers
   ↓ Basic Authentication(Honoミドルウェア)
アプリケーションロジック
```

認証処理はミドルウェアとして一箇所にまとめ、アプリケーション本体のルーティングに複雑なログイン・セッション機構を持たせない。

### 21.3 パスワード

認証情報はソースコードにハードコードしない。

Wrangler secrets(`wrangler secret put`)等の安全な方法で管理する。

### 21.4 リソースへの影響

Basic AuthenticationはWorkerのCPU時間制限に対して十分軽量であることを前提とする。

---

## 22. 外部ドメイン

Webアプリには外部公開用の独自サブドメインを使用する。

ドメイン名は別途ユーザーが指定する。対象ドメインのDNSがCloudflareで管理されていることを前提とし、Cloudflare Workersの**Custom Domains**機能でサブドメインをWorkerに割り当てる。

要求仕様・ソースコードに特定の固定ドメインをハードコードしない。

---

## 23. エラー処理

エラーは処理全体を停止させないことを基本とする。

### 23.1 Google Playエラー

- 前回データ保持
- エラー記録
- 次回再試行

### 23.2 Geminiエラー

- 取得済みGoogle Playデータは保持
- AI分析のみ失敗扱い
- 未実施状態を保存
- 後から再実行可能

### 23.3 ネットワークエラー

タイムアウトを設定し、1つのアプリの通信失敗によって全アプリの処理が停止しないようにする。

---

## 24. 性能・リソース要件

Cloudflare Workers無料プランの制約(リクエストあたりCPU時間・1日あたりリクエスト数・サブリクエスト数の上限等)を考慮し、以下を基本方針とする。

- ローカルLLMを使用しない
- Durable Objects・Queues等の有料機能を前提としない
- Cloudflare D1を使用する
- 画像バイナリを保存しない
- 必要なデータだけをGeminiへ送信する
- 1回の呼び出し(Cron発火・HTTPリクエスト)で処理するアプリ数を、CPU時間制限に収まる範囲に制限し、必要に応じて複数回に分割する
- Google Play取得処理を大量並列化しない(サブリクエスト数上限にも注意する)
- D1への書き込み行数を無制限に増やさない

100アプリ程度の監視を前提に、逐次処理または少数の分割処理で安定動作する設計とする。無料プランの各種上限は将来変更されうるため、具体的な数値は実装時に最新の提供条件を確認する。

---

## 25. 設定管理

以下のような項目はソースコードに直接埋め込まず、Wranglerの環境変数(`vars`)またはシークレット(`secrets`)で変更可能にする。

- Google Gemini API Key
- Geminiモデル名
- 外部ドメイン(Custom Domain設定)
- Basic Authentication用の認証情報
- 監視対象上限
- 定期実行時刻(Cron Triggerの`crons`設定)
- レビュー保持期間
- ログ(実行履歴)保持期間

API Key等の秘密情報はGitリポジトリへコミットせず、Wrangler secretsで管理する。

---

## 26. 想定ディレクトリ構成

実装時の一例:

```text
/
├── src/
│   ├── index.ts
│   ├── config.ts
│   ├── collector/
│   ├── ai/
│   ├── db/
│   ├── jobs/
│   └── web/
├── migrations/
├── wrangler.jsonc
├── package.json
├── tsconfig.json
└── README.md
```

実装上より適切な構成があれば変更してよい。

---

## 27. Web UIの最低限の画面

### 27.1 ダッシュボード

- 監視中アプリ数 / 100
- 停止中アプリ数
- 最終取得日時
- 最終AI分析日時
- エラー件数
- D1ストレージ使用量(概算)
- 次回定期実行日時
- 最近の更新・変化

### 27.2 アプリ一覧

- アプリ名
- アイコン
- 評価
- レビュー件数
- バージョン
- 更新日
- 最終取得日時
- 状態

### 27.3 アプリ詳細

- 現在情報
- 履歴
- 差分
- レビュー
- What's New
- AI分析
- エラー履歴

### 27.4 アプリ登録

パッケージ名を入力して登録する。

### 27.5 設定

- Geminiモデル
- 定期実行設定
- その他システム設定

### 27.6 AI Q&A

自然言語で監視データについて質問できる。

---

## 28. 運用上の基本方針

本システムは「Cloudflareの無料プランの範囲内で長期間放置して運用できること」を重視する。

そのため、

1. データ取得とAI分析を分離する
2. 1アプリの失敗で全体を止めない
3. 過去データを不用意に削除しない
4. 無料枠超過で課金しない(Gemini・Cloudflareいずれも)
5. デプロイ後も冪等なジョブ設計により安定動作する
6. D1のバックアップ・復元手段(Time Travel等)を把握しておく
7. D1のストレージ使用量を監視する
8. 実行履歴(ログ相当)を無制限に増やさない
9. 手動実行できるようにする
10. システムの最終実行状態をWeb UIから確認できるようにする

ことを重要な設計原則とする。

---

## 29. 非機能要件

### 可用性

- サーバーレスのためVM再起動という概念がなく、Cloudflareのインフラ可用性に依存する
- デプロイ失敗時はロールバック可能とする
- 一部アプリの取得失敗で全体を停止しない
- Gemini停止時もデータ収集を継続

### 保守性

- Google Play Collectorを独立モジュール化
- Geminiモデルを設定変更可能にする
- 設定値をコードから分離(Wrangler vars/secrets)
- D1の実行履歴テーブルで障害原因を追跡可能にする

### セキュリティ

- HTTPS必須(Cloudflareが自動提供)
- Worker内Basic Authentication
- API Key・認証情報をソースコードに含めない(Wrangler secretsで管理)
- 不要なエンドポイントを外部公開しない

### リソース効率

- Cloudflare Workers/D1の無料プラン内で安定稼働
- 画像・動画等の大容量バイナリを保存しない
- 1回の呼び出しで処理するアプリ数をCPU時間制限内に収める

---

## 30. 将来拡張候補

初期バージョンでは実装しないが、将来的に以下を追加できる構造にする。

- Discord通知
- メール通知
- Slack通知
- 特定条件によるアラート
- グラフの高度化
- AI分析の種類追加
- AIによる自動レポート生成
- 監視対象100アプリ以上への拡張
- 複数ユーザー対応
- より高度な認証
- Cloudflare Workers Paidプランへの移行(Queues/Durable Objects/Logpush等の活用)
- R2等を用いた外部ストレージへのバックアップ

将来拡張のために過度な設計を行い、現在の無料プランの制約を圧迫してはならない。

---

## 31. 完成条件

以下を満たした状態を初期リリースの完成とする。

- [ ] パッケージ名でアプリを登録できる
- [ ] 最大100アプリを監視できる
- [ ] 1日1回自動取得できる(複数回のCron発火に分割してもよい)
- [ ] 手動取得できる
- [ ] Google Play公開情報を取得できる
- [ ] 日次履歴をD1へ保存できる
- [ ] 前回との差分を確認できる
- [ ] 直近90日レビューを管理できる
- [ ] 画像バイナリを保存しない
- [ ] Gemini 2.5 Flash-Lite等を利用してAI分析できる
- [ ] Gemini無料枠超過時に課金へ移行しない
- [ ] 日次AI分析を実行できる(複数回のCron発火に分割してもよい)
- [ ] Web UIからAI Q&Aを利用できる
- [ ] アプリの監視停止・再開ができる
- [ ] 停止アプリの履歴を保持・閲覧できる
- [ ] 永久削除が確認付きで実行できる
- [ ] Google Playから消えたアプリを自動削除しない
- [ ] 取得・AI分析のエラー状態を確認できる
- [ ] Cloudflare無料プランの範囲内で運用できる
- [ ] D1のバックアップ・復元手段(Time Travel等)を確認できる
- [ ] D1の実行履歴(ログ相当)を無制限に増やさない
- [ ] D1のストレージ使用量を確認できる
- [ ] HTTPSでアクセスできる(Cloudflare提供)
- [ ] Worker内Basic Authenticationを利用できる
- [ ] API Key等の秘密情報を安全に管理できる(Wrangler secrets)

---

## 32. 実装時の重要事項

この要求仕様はVibe Codingによる実装を想定する。

実装者は、要求を満たすために必要な技術的判断を行ってよいが、以下は勝手に変更しないこと。

- 監視対象はユーザー指定方式
- 最大100アプリ
- 1日1回の定期取得(複数回のCron発火への分割は可)
- レビュー保持は90日
- 画像バイナリは保存しない
- Cloudflare D1を使用
- Gemini APIを使用
- 無料枠超過時に課金しない(Gemini・Cloudflareいずれも)
- 日次AI分析
- Web UI AI Q&A
- Worker内Basic Authentication + Cloudflare提供のHTTPS
- データ取得とAI分析を分離
- アプリ履歴を保持
- Google Playから消えたアプリを自動削除しない
- D1のバックアップ・復元手段(Time Travel等)を把握する
- ストレージ使用量・実行履歴(ログ相当)の管理
- **VM/VPSを常時稼働させない。Cloudflare Workers Paidプラン等の有料機能を前提としない**

特に、Cloudflare Workers無料プランのCPU時間制限・リクエスト数上限等を常に考慮し、1回の呼び出しで処理する範囲を無理に広げないこと。
